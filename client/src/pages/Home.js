import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
    width: 100%;
    padding: 1rem;
`;

const Home = ({history}) => {
    return (
        <Wrapper>
            Home
        </Wrapper>
    )
};

export default Home;